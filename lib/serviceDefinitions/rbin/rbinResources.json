{
    "rule": {
        "resource": "rule",
        "arn": "arn:${Partition}:rbin:${Region}:${Account}:rule/${ResourceName}",
        "conditionKeys": [
            "aws:ResourceTag/${TagKey}"
        ]
    }
}