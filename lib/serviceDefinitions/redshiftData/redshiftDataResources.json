{
    "cluster": {
        "resource": "cluster",
        "arn": "arn:${Partition}:redshift:${Region}:${Account}:cluster:${ClusterName}",
        "conditionKeys": [
            "aws:ResourceTag/${TagKey}"
        ]
    }
}